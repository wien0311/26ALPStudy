#include <stdio.h>
#include <string.h>

int main() {
    char editor[100001],arr[5];
    int m=0, cl;

    scanf("%s",editor);
    scanf("%d",&m);
    cl=strlen(editor)+1;

    for(int i=0; i<m; i++) {
        scanf("%s",arr);
        if(arr[0]=='L') {

        }
        else if(arr[0]=='D') {

        }
        else if(arr[0]=='B') {

        }
        else if(arr[0]=='P') {

        }
    }
}